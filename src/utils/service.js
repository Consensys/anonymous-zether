const service = {};

service.proveTransfer = (CL, CR, y, epoch, x, bTransfer, bDiff, index) => {
    // todo...
}

service.proveBurn = (CL, CR, y, bTransfer, epoch, x, bDiff) => {
    // todo...
}


module.exports = service;